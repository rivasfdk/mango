<%= render_error_messages(f.error_messages) %>
<% disabled = (edit) ? true : false %>
<label for="number" class="small">Número:</label>
<div class="field"><%= f.text_field :number, :size=>'10', :disabled => disabled %> <span>*</span></div>
<label for="number" class="small">Nombre:</label>
<div class="field"><%= f.text_field :name, :size=>'10' %></div>
<% if edit %>
<label for="scale_id" class="small">Balanza:</label>
<div class="field"><%= f.collection_select :scale_id, @scales, :id, :name, {:include_blank => true}, :class=>'chzn-select chzn-field-normal' %> <span>*</span></div>
<% end %>
<label for="ingredient" class="small">Ingrediente (Lote):</label>
<% if @current_lot.nil? %>
<div class="field"><%= f.collection_select :hopper_lot, @lots, :id, :to_collection_select, {:include_blank => true}, :class=>'chzn-select chzn-field-normal' %> <span>*</span></div>
<% else %>
<div class="field"><%= f.collection_select :hopper_lot, @lots, :id, :to_collection_select, {:include_blank => true, :selected => @current_lot.id}, :class=>'chzn-select chzn-field-normal' %> <span>*</span></div>
<% end %>
<div class="clearfix"></div>
<br/><span class="required">* Campos obligatorios</span>
