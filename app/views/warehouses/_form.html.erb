<%= render_error_messages(f.error_messages) %>
<label for="code" class="small">Código:</label>
<div class="field"><%= f.text_field :code, :size=>'20' %> <span>*</span></div>
<br /><br />
<label for="name" class="small">Nombre:</label>
<div class="field"><%= f.text_field :name, :size=>'20' %> <span>*</span></div>
<br /><br />
<% if @warehouse_types.name.downcase == "materia prima" %>
  <label for="ingredient" class="small">Materia Prima:</label>
  <div class="field"><%= f.collection_select :ingredient_id, Ingredient.all, :id, :to_collection_select, {include_blank: "Ninguno" }, :class=>'chosen-select chzn-field-normal' %> <span>*</span></div>
<% elsif @warehouse_types.name.downcase == "producto terminado" %>
  <label for="product" class="small">Producto:</label>
  <div class="field"><%= f.collection_select :product_id, Product.all, :id, :to_collection_select, {include_blank: "Ninguno" }, :class=>'chosen-select chzn-field-normal' %> <span>*</span></div>
<% else %> 
  <label for="ingredient" class="small">Materia Prima:</label>
  <div class="field"><%= f.collection_select :ingredient_id, Ingredient.all, :id, :to_collection_select, {include_blank: "Ninguno" }, :class=>'chosen-select chzn-field-normal' %></div>
  <label for="product" class="small">Producto:</label>
  <div class="field"><%= f.collection_select :product_id, Product.all, :id, :to_collection_select, {include_blank: "Ninguno" }, :class=>'chosen-select chzn-field-normal' %></div>
<% end %>
<br /><br />
<label for="size" class="small">Capacidad:</label>
<div class="field"><%= f.text_field :size, :size=>'20' %> </div>
<div class="clearfix"></div>
<br/><span class="required">* Campos obligatorios</span>