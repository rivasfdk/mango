<%= render_error_messages(f.error_messages) %>
<% if @hoppers_transactions_enabled %>
<label for="ci" class="small">Nivel mínimo en tolvas (%):</label>
<div class="field"><%= f.text_field :hopper_minimum_level, :size=>'20' %><span>*</span></div>
<% end %>
<br /><br />
<% if @romano_enabled %>
  <label for="ci" class="small">Diferencia de peso máxima recepción (%):</label>
  <div class="field"><%= f.text_field :ticket_reception_diff, :size=>'20' %><span>*</span></div>
  <label for="ci" class="small">Diferencia de peso máxima despacho (%):</label>
  <div class="field"><%= f.text_field :ticket_dispatch_diff, :size=>'20' %><span>*</span></div>

  <label for="sacks" class="small">Puerto 1: </label>
  <div class="field"><%= f.check_box :port1 %></div><br />
  <label for="sacks" class="small">Puerto 2: </label>
  <div class="field"><%= f.check_box :port2 %></div><br />

<% end %>
<div class="clearfix"></div>
<br/><span class="required">* Campos obligatorios</span>
